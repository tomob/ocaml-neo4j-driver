(* Configuration records for the Neo4j driver.

   Phase A0 (see PLAN.md) will define driver / session / transaction config
   records with defaults, e.g. connection_acquisition_timeout, max_transaction_retry_time,
   initial_retry_delay, retry_delay_multiplier, retry_delay_jitter_factor,
   fetch_size, max_pool_size and max_connection_lifetime. *)
