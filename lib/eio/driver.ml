(* Entry point of the Eio backend of the Neo4j driver.

   Phase A2 (see PLAN.md) will add the Eio transport (net/clock/sw plumbing),
   followed by handshake, TLS and the connection/session/pool layers. *)
